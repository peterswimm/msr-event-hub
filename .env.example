# ========================================
# BACKEND - NON-SENSITIVE CONFIG
# ========================================

# Environment
ENVIRONMENT=production
LOG_LEVEL=INFO
DEBUG=false

# LLM Provider (foundry recommended)
LLM_PROVIDER=foundry

# Azure OpenAI Endpoints (NOT secrets - just service endpoints)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Azure Foundry Configuration
FOUNDRY_PROJECT_ENDPOINT=https://your-project.api.azureml.ms/
FOUNDRY_MODEL_DEPLOYMENT=gpt-4o

# Agent Framework Settings
AGENT_TEMPERATURE=0.3
AGENT_MAX_TOKENS=4096
AGENT_TIMEOUT=300

# Observability
ENABLE_TRACING=true
OTLP_ENDPOINT=http://localhost:4317
ENABLE_SENSITIVE_DATA=true

# POC Workflow Settings
POC_MINIMUM_EXPERT_RATING=3.0
POC_REQUIRE_HUMAN_APPROVAL=false
POC_MAX_ITERATIONS=2
POC_ENABLE_COMPILATION=false

# Evaluation
EVALUATION_OUTPUT_DIR=./outputs/evaluation
EVALUATION_BATCH_SIZE=10

# Workflow Orchestration
MAX_CONCURRENT_AGENTS=5
GROUP_CHAT_MAX_ROUNDS=10

# File Paths
INPUTS_DIR=./inputs
OUTPUTS_DIR=./outputs

# ========================================
# AZURE KEY VAULT INTEGRATION
# ========================================
# Key Vault URL where secrets are stored
# Example: https://kv-event-hub-backend-prod.vault.azure.net/
KEY_VAULT_URL=https://kv-event-hub-backend-prod.vault.azure.net/

# Azure Tenant ID for authentication
AZURE_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ========================================
# SECRETS (STORED IN KEY VAULT, NOT HERE!)
# ========================================
# The following secrets must be created in Azure Key Vault:
#
# azure-openai-api-key
#   - Azure OpenAI API key for LLM calls
#   - Rotation: As needed
#
# database-connection-string
#   - PostgreSQL connection string with password
#   - Rotation: 90 days
#
# encryption-master-key
#   - Master key for data encryption at rest
#   - Rotation: Rarely (key versioning recommended)
#
# redis-password
#   - Redis authentication password
#   - Rotation: As needed
#
# foundry-api-key
#   - Azure Foundry API key (if not using Managed Identity)
#   - Rotation: As needed
#
# DO NOT ADD ANY SECRETS TO THIS FILE!
